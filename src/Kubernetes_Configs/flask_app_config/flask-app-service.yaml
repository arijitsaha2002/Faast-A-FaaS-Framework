apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  namespace: flask-app-namespace
spec:
  selector:
    app: flask-app 
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8080 
      targetPort: 5000
      nodePort: 30004
